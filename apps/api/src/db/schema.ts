// Real schema lands in T-202 (spec §3.3 — users, wallets, sessions, api_keys,
// off_chain_policies, service_catalog, audit_log, dodo_payments,
// treasury_disbursements). This file exists so drizzle-kit can resolve the
// schema reference in drizzle.config.ts.

export {};
