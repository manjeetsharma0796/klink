# Backend HTTP API

Express + TypeScript + Drizzle + Postgres + Redis. Implements the `/v1/*` surface from [design spec §3.2](../../docs/specs/2026-04-28-agent-wallet-design.md).

**Status:** placeholder. Real scaffold (TS strict + Express + Drizzle + dotenv + vitest + booting `/health`) lands when [`T-201`](../../TODO.md) is claimed and completed.
