{
  "name": "klink",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "bun --filter @klink/api dev",
    "test": "bun --filter '*' test",
    "typecheck": "bun --filter '*' typecheck",
    "lint": "biome check .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
