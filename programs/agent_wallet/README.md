# `agent_wallet` Anchor program

On-chain Anchor program implementing the vault PDA, session accounts, `transfer_usdc` with all reverts, `kamino_deposit` / `kamino_withdraw` CPIs, and the typed-instruction validator. See [design spec §2](../../docs/specs/2026-04-28-agent-wallet-design.md).

**Status:** placeholder. Real scaffold (Anchor workspace, `lib.rs`, `Cargo.toml`, `Anchor.toml`, IDL) lands when [`T-102`](../../TODO.md) is claimed and completed.
